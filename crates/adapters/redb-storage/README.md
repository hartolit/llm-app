# redb-storage

ACID desktop persistence for settings and the logical model catalogue. Records
use explicit binary headers and versions; cache paths are intentionally resolved
from repository and revision on startup instead of persisted.
